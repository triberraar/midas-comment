'use strict';

var dependencies = [
	'ngResource',
	'ngRoute',
	'ngMessages',
	'error',
	'comments.config'
    
];

angular.module('comments.app', dependencies)
    .config(function () {
    	
    })
    .run(function () {
    });