'use strict';

var dependencies = [
	'ngResource',
	'ngRoute',
	'comments.config'
    
];

angular.module('comments.app', dependencies)
    .config(function () {
    	
    })
    .run(function () {
    });